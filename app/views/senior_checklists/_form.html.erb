<%= form_for(@senior_checklist) do |f| %>
  <% if @senior_checklist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@senior_checklist.errors.count, "error") %> prohibited this senior_checklist from being saved:</h2>

      <ul>
      <% @senior_checklist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :month %><br>
    <%= f.text_field :month %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
